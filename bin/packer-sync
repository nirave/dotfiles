#!/bin/bash
# Wrapper to sync packer plugins in headless mode
export XDG_CONFIG_HOME="$HOME/.config/config_niravim"
export XDG_DATA_HOME="$HOME/.local/share/nvim-niravim"
nvim --headless -c 'autocmd User PackerComplete quitall' -c 'PackerSync'
